tests:
- as: deploy-pulp-on-openshift
  steps:
    allow_skip_on_success: true
    test:
    - as: deploy-pulp
      commands: |
        echo "it works"
      resources:
        requests:
          cpu: 1000m
          memory: 4Gi
