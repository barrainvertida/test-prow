base_images:
  python:
    name: "python"
    namespace: "openshift"
    tag: "3.8"
tests:
- as: verify
  steps:
    test:
    - as: "test1"
      from: python
      commands: |
        "print(it works!)"
      resources:
        requests:
          cpu: 100m
          memory: 100Mi
